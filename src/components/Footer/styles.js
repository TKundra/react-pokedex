import styled from "styled-components";

const Container = styled.div`
    bottom: 0;
    right: 0;
    left: 0;
    position: absolute;
    position: fixed;
    padding: 20px 50px;
    display: flex;
    justify-content: space-between;
`;

export {
    Container
}